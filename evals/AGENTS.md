# Evaluation Instructions

Follow the root `AGENTS.md`. Store versioned, anonymized input cases and expected outcomes in `cases/`; write generated, non-source-of-truth output to `reports/`.

Cover valid, ambiguous, unsafe, unsupported, and hallucination-prone questions. Keep evaluation assertions meaningful—never reduce coverage or acceptance criteria merely to obtain a passing result.
