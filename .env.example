# Application
APP_ENV=
APP_HOST=
APP_PORT=

# PostgreSQL (use a read-only runtime role for generated SQL)
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Model provider configuration
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=
