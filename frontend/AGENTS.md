# Frontend Instructions

Follow the root `AGENTS.md`. Use React and TypeScript when implementation begins. Keep API clients in `src/api`, reusable UI in `src/components`, user flows in `src/features`, and shared contracts in `src/types`.

Present generated SQL, confidence, limitations, and blocked-query outcomes clearly; the UI must not imply that unsafe or uncertain output was executed.
